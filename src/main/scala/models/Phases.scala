package models

case class Phases(
                   move: Boolean,
                   rangeAttack: Boolean,
                   closeCombatAttack: Boolean,
                   victoryCheck: Boolean
                 )
